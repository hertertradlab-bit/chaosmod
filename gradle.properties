# NOTE: MC 26.2 ("Chaos Cubed") is a very recent year-based drop. Fabric's
# exact published version strings for it may differ slightly from these
# placeholders. Before building, check https://fabricmc.net/develop/ (or the
# Fabric meta API: https://meta.fabricmc.net/v2/versions) and update:
#   minecraft_version, yarn_mappings, loader_version, fabric_version
# to whatever it lists for "26.2". Everything else below is stable.

# Minecraft / mappings / loader
minecraft_version=26.2
yarn_mappings=26.2+build.1
loader_version=0.16.9

# Mod properties
mod_version=1.0.0
maven_group=com.chaosmod
archives_base_name=chaosmod

# Dependencies
fabric_version=0.100.0+26.2
